typedef enum {
	WRITE,
	TRUNCATE,
	READ_LAYOUT,
	CLONE_FROM,
	CLONE_TO,
	DELETE_RANGE,
	GET_INODE,
	GET_INODE_WHOLE,
	SET_INODE,
	CREATE_INODE,
	DELETE_INODE,
	REMOVE_DOBJ,
	READ_DOBJ,
	BACKUP_AND_STOP,
	STAGE_OUT_ALL,
	GET_INODE_DOBJ
} INS_COM;

typedef enum {
	SERVER_BUSY,
	QUEUED,
	SUCCESS,
	FAILED,
	INVALID_INO,
	INVALID_DOBJECT,
	INTERNAL_ERROR,
	INVALID_RANGE,
	INVALID_RANGE_CLONE,
	CREATE_INODE_FAIL,
	SHARED_BASE,
	EXTENT_EXIST
} INS_RET;
